@model IEnumerable<ModernRecrut.MVC.Models.Postulations.Postulation>

@{
    ViewData["Title"] = "Index";
}

<h1>Liste de vos postulations</h1>
<h4>@User.Identity.Name</h4>
<hr />

<br />

@*Bloc du filtrage*@
@*<div class="d-flex justify-content-left">
    <form asp-action="Index" asp-controller="OffresEmplois" method="post">
        <div class="row g-3 align-items-center">
            <div class="col-auto">
                <input class="form-control" type="text" id="filtre" name="filtre" placeholder="Rechercher une offre" />
            </div>
            <div class="col-auto">
                <input class="btn btn-primary" type="submit" value="Rechercher" />
            </div>

        </div>
    </form>
</div>

<br />
*@

<table class="table table-striped table-dark table-bordered text-center mt-1 align-middle">
    <thead>
        <tr>
            <th>
                Nom/Prénom
            </th>
            <th>
                Offre d'emploi
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PretentionSalariale)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateDisponibilite)
            </th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Candidat.Prenom) @Html.DisplayFor(modelItem => item.Candidat.Nom)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.OffreEmploi.Nom)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PretentionSalariale)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DateDisponibilite)
                </td>
                <td>
                    <a class="text-warning" asp-action="Details" asp-controller="OffresEmplois" 
                    asp-route-id="@item.IdOffreEmploi" target="_blank">
                    Consulter l'offre d'emploi
                </a>
                </td>
            </tr>
        }
    </tbody>
</table>